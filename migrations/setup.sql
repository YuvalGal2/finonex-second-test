CREATE DATABASE events;

CREATE TABLE users_revenue (
                              user_id VARCHAR(255),
                              revenue INTEGER
);
INSERT INTO users_revenue (user_id, revenue) VALUES ('user1', 20);
